//Rules Button Alert// 

document.getElementById("rules").addEventListener("click", showRules);
function showRules() {
  alert("Click on as many faces as possible (only one click counts per image) in under 20 seconds. Good Luck!");
  
}